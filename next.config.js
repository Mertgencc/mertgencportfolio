/** @type {import('next').NextConfig} */
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Eski hatalı config:
  // experimental: { appDir: true }
  
  // Yeni doğru config:
  appDir: true
}

module.exports = nextConfig


